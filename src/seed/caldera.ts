// GENERATED by scripts/gen-caldera-seed.mjs from docs/case-study/caldera/document — do not edit by hand;
// regenerate with `node scripts/gen-caldera-seed.mjs`. The Caldera LBO case-study
// document inlined as plain string constants for memoryReader (the seed portability
// rule: no bundler-specific ?raw / import.meta.glob, which break on immediately.run).
// The live demo branch serves this as the app's document (src/App.tsx → CALDERA_SEED).

export const CALDERA_ROOT = 'caldera';

export const CALDERA_FILES: Record<string, string> = {
  "caldera/fixtures/assumptions.frame.json": "{\n  \"rows\": [\n    {\n      \"entry_multiple\": 8.0,\n      \"exit_multiple\": 8.0,\n      \"hold_years\": 5,\n      \"tlb_turns\": 5.0,\n      \"mezz_turns\": 1.0,\n      \"revolver_capacity\": 25.0,\n      \"revolver_rate\": 0.09,\n      \"commitment_fee\": 0.005,\n      \"tlb_rate\": 0.08,\n      \"tlb_amort_pct\": 0.01,\n      \"mezz_cash_rate\": 0.12,\n      \"mezz_pik_rate\": 0.02,\n      \"cash_sweep_pct\": 0.75,\n      \"existing_debt\": 55.0,\n      \"existing_cash\": 10.0,\n      \"min_cash\": 8.0,\n      \"txn_fee_pct\": 0.02,\n      \"fin_fee_pct\": 0.025,\n      \"tax_rate\": 0.25,\n      \"da_pct\": 0.045,\n      \"nwc_pct\": 0.15,\n      \"hurdle_irr\": 0.2\n    }\n  ],\n  \"provenance\": {\n    \"synthetic\": true,\n    \"captureActor\": \"generate.py\",\n    \"capturedAt\": \"2026-07-09\",\n    \"note\": \"deal assumptions; the three deal knobs are mirrored as params\"\n  }\n}\n",
  "caldera/fixtures/expected_holdout.frame.json": "{\n  \"rows\": [\n    {\n      \"fy2031_revenue\": 263.1630898332,\n      \"fy2031_ebitda\": 57.40330402427402,\n      \"fy2027_revenue\": 235.53\n    }\n  ],\n  \"provenance\": {\n    \"synthetic\": true,\n    \"captureActor\": \"generate.py\",\n    \"capturedAt\": \"2026-07-09\",\n    \"note\": \"Python-verified operating build under the holdout plan (the ops_holdout oracle)\"\n  }\n}\n",
  "caldera/fixtures/expected_values.frame.json": "{\n  \"rows\": [\n    {\n      \"ltm_ebitda\": 46.27,\n      \"entry_ev\": 370.16,\n      \"sponsor_equity\": 114.8837,\n      \"uses_total\": 394.50370000000004,\n      \"exit_ev\": 516.9061099614241,\n      \"exit_equity\": 315.45910005498115,\n      \"moic\": 2.7458995493266767,\n      \"irr\": 0.22387462120837076,\n      \"irr_avg\": 0.2251537582183193,\n      \"breakeven_exit_multiple\": 7.5420183108582,\n      \"tlb_end_2031\": 173.71651835678423,\n      \"cash_end_2031\": 23.355327214405253,\n      \"net_debt_2031\": 201.447009906443,\n      \"fy2031_revenue\": 289.72619642520004,\n      \"fy2031_ebitda\": 64.61326374517802,\n      \"net_debt_2031_avg\": 199.79504427119048\n    }\n  ],\n  \"provenance\": {\n    \"synthetic\": true,\n    \"captureActor\": \"generate.py\",\n    \"capturedAt\": \"2026-07-09\",\n    \"note\": \"Python-verified spot values (the specification-test oracle)\"\n  }\n}\n",
  "caldera/fixtures/historical_segments.frame.json": "{\n  \"rows\": [\n    {\n      \"segment\": \"seals\",\n      \"fy2024\": 86.2,\n      \"fy2025\": 91.0,\n      \"fy2026\": 96.0,\n      \"margin_fy2026\": 0.26\n    },\n    {\n      \"segment\": \"bearings\",\n      \"fy2024\": 70.1,\n      \"fy2025\": 72.1,\n      \"fy2026\": 74.0,\n      \"margin_fy2026\": 0.19\n    },\n    {\n      \"segment\": \"fabrication\",\n      \"fy2024\": 60.5,\n      \"fy2025\": 59.2,\n      \"fy2026\": 58.0,\n      \"margin_fy2026\": 0.125\n    }\n  ],\n  \"provenance\": {\n    \"synthetic\": true,\n    \"captureActor\": \"generate.py\",\n    \"capturedAt\": \"2026-07-09\",\n    \"note\": \"FY2024-26 segment revenue + FY2026 margins (LTM at close)\"\n  }\n}\n",
  "caldera/fixtures/ops_plan.frame.json": "{\n  \"rows\": [\n    {\n      \"segment\": \"seals\",\n      \"year\": 2027,\n      \"growth\": 0.08,\n      \"margin\": 0.264\n    },\n    {\n      \"segment\": \"seals\",\n      \"year\": 2028,\n      \"growth\": 0.08,\n      \"margin\": 0.268\n    },\n    {\n      \"segment\": \"seals\",\n      \"year\": 2029,\n      \"growth\": 0.07,\n      \"margin\": 0.272\n    },\n    {\n      \"segment\": \"seals\",\n      \"year\": 2030,\n      \"growth\": 0.07,\n      \"margin\": 0.276\n    },\n    {\n      \"segment\": \"seals\",\n      \"year\": 2031,\n      \"growth\": 0.06,\n      \"margin\": 0.28\n    },\n    {\n      \"segment\": \"bearings\",\n      \"year\": 2027,\n      \"growth\": 0.04,\n      \"margin\": 0.192\n    },\n    {\n      \"segment\": \"bearings\",\n      \"year\": 2028,\n      \"growth\": 0.04,\n      \"margin\": 0.194\n    },\n    {\n      \"segment\": \"bearings\",\n      \"year\": 2029,\n      \"growth\": 0.04,\n      \"margin\": 0.196\n    },\n    {\n      \"segment\": \"bearings\",\n      \"year\": 2030,\n      \"growth\": 0.035,\n      \"margin\": 0.198\n    },\n    {\n      \"segment\": \"bearings\",\n      \"year\": 2031,\n      \"growth\": 0.035,\n      \"margin\": 0.2\n    },\n    {\n      \"segment\": \"fabrication\",\n      \"year\": 2027,\n      \"growth\": 0.025,\n      \"margin\": 0.127\n    },\n    {\n      \"segment\": \"fabrication\",\n      \"year\": 2028,\n      \"growth\": 0.025,\n      \"margin\": 0.129\n    },\n    {\n      \"segment\": \"fabrication\",\n      \"year\": 2029,\n      \"growth\": 0.02,\n      \"margin\": 0.131\n    },\n    {\n      \"segment\": \"fabrication\",\n      \"year\": 2030,\n      \"growth\": 0.02,\n      \"margin\": 0.133\n    },\n    {\n      \"segment\": \"fabrication\",\n      \"year\": 2031,\n      \"growth\": 0.02,\n      \"margin\": 0.135\n    }\n  ],\n  \"provenance\": {\n    \"synthetic\": true,\n    \"captureActor\": \"generate.py\",\n    \"capturedAt\": \"2026-07-09\",\n    \"note\": \"management plan: growth + margin per segment-year\"\n  }\n}\n",
  "caldera/fixtures/ops_plan_holdout.frame.json": "{\n  \"rows\": [\n    {\n      \"segment\": \"seals\",\n      \"year\": 2027,\n      \"growth\": 0.06,\n      \"margin\": 0.259\n    },\n    {\n      \"segment\": \"seals\",\n      \"year\": 2028,\n      \"growth\": 0.06,\n      \"margin\": 0.263\n    },\n    {\n      \"segment\": \"seals\",\n      \"year\": 2029,\n      \"growth\": 0.05,\n      \"margin\": 0.267\n    },\n    {\n      \"segment\": \"seals\",\n      \"year\": 2030,\n      \"growth\": 0.05,\n      \"margin\": 0.271\n    },\n    {\n      \"segment\": \"seals\",\n      \"year\": 2031,\n      \"growth\": 0.039999999999999994,\n      \"margin\": 0.275\n    },\n    {\n      \"segment\": \"bearings\",\n      \"year\": 2027,\n      \"growth\": 0.02,\n      \"margin\": 0.187\n    },\n    {\n      \"segment\": \"bearings\",\n      \"year\": 2028,\n      \"growth\": 0.02,\n      \"margin\": 0.189\n    },\n    {\n      \"segment\": \"bearings\",\n      \"year\": 2029,\n      \"growth\": 0.02,\n      \"margin\": 0.191\n    },\n    {\n      \"segment\": \"bearings\",\n      \"year\": 2030,\n      \"growth\": 0.015000000000000003,\n      \"margin\": 0.193\n    },\n    {\n      \"segment\": \"bearings\",\n      \"year\": 2031,\n      \"growth\": 0.015000000000000003,\n      \"margin\": 0.195\n    },\n    {\n      \"segment\": \"fabrication\",\n      \"year\": 2027,\n      \"growth\": 0.005000000000000001,\n      \"margin\": 0.122\n    },\n    {\n      \"segment\": \"fabrication\",\n      \"year\": 2028,\n      \"growth\": 0.005000000000000001,\n      \"margin\": 0.124\n    },\n    {\n      \"segment\": \"fabrication\",\n      \"year\": 2029,\n      \"growth\": 0.0,\n      \"margin\": 0.126\n    },\n    {\n      \"segment\": \"fabrication\",\n      \"year\": 2030,\n      \"growth\": 0.0,\n      \"margin\": 0.128\n    },\n    {\n      \"segment\": \"fabrication\",\n      \"year\": 2031,\n      \"growth\": 0.0,\n      \"margin\": 0.13\n    }\n  ],\n  \"provenance\": {\n    \"synthetic\": true,\n    \"captureActor\": \"generate.py\",\n    \"capturedAt\": \"2026-07-09\",\n    \"note\": \"the downside holdout plan (growth -2pp, margins -0.5pp) \\u2014 the substitution fixture for the ops_holdout test (R3-373)\"\n  }\n}\n",
  "caldera/fixtures/year_plan.frame.json": "{\n  \"rows\": [\n    {\n      \"year\": 2027,\n      \"capex_pct\": 0.055\n    },\n    {\n      \"year\": 2028,\n      \"capex_pct\": 0.05\n    },\n    {\n      \"year\": 2029,\n      \"capex_pct\": 0.045\n    },\n    {\n      \"year\": 2030,\n      \"capex_pct\": 0.04\n    },\n    {\n      \"year\": 2031,\n      \"capex_pct\": 0.04\n    }\n  ],\n  \"provenance\": {\n    \"synthetic\": true,\n    \"captureActor\": \"generate.py\",\n    \"capturedAt\": \"2026-07-09\",\n    \"note\": \"company-level plan vectors (capex % of revenue)\"\n  }\n}\n",
  "caldera/reckoner.json": "{\n  \"format\": 1,\n  \"compat\": { \"stdlib\": \">=0.1.0\", \"catalog\": \">=0.1.0\" },\n  \"authoredWith\": { \"app\": \"reckoner\", \"stdlib\": \"0.1.0\", \"catalog\": \"0.1.0\" },\n  \"worksheets\": [\"model\", \"checks\"],\n  \"params\": { \"entry_multiple\": 8, \"exit_multiple\": 8, \"tlb_turns\": 5 },\n  \"paramRefs\": {\n    \"tax_rate\": { \"from\": \"fixtures.assumptions\", \"path\": \"0.tax_rate\" },\n    \"cash_sweep_pct\": { \"from\": \"fixtures.assumptions\", \"path\": \"0.cash_sweep_pct\" }\n  },\n  \"title\": \"Caldera Components — LBO\"\n}\n",
  "caldera/templates/deal_summary.mdx": "# Caldera Components — LBO.\n\nThe Harrow Capital take-private of Caldera Components (fictional): entry at\n<Value source=\"params.entry_multiple\" />x LTM EBITDA, exit at\n<Value source=\"params.exit_multiple\" />x, funded with\n<Value source=\"params.tlb_turns\" />x Term Loan B plus a 1.0x mezzanine tranche.\nDebt schedule charges interest on beginning balances; the converged\naverage-balance variant lives in the workbook cells.\n\n<Params>\n  <Range name=\"entry_multiple\" min={6} max={10} step={0.5} default={8} />\n  <Range name=\"exit_multiple\" min={6} max={10} step={0.5} default={8} />\n  <Range name=\"tlb_turns\" min={3.5} max={6.5} step={0.5} default={5} />\n  <Range name=\"tax_rate\" min={0.15} max={0.35} step={0.01} default={0.25} />\n</Params>\n\n<Row>\n  <Kpi source=\"model.sponsor_irr\" format=\"percent\" />\n  <Kpi source=\"model.moic\" format=\"multiple\" />\n  <Kpi source=\"model.sponsor_equity\" format=\"currency\" unit=\"m\" />\n</Row>\n\n## The deleveraging story.\n\n<Chart source=\"model.debt_schedule\" kind=\"bar\" x=\"year\" y=\"tlb_end\" />\n<Chart source=\"model.operating\" kind=\"line\" x=\"year\" y=\"ebitda\" />\n\n<Table source=\"model.debt_schedule\" columns={[\"year\", \"ebitda\", \"interest\", \"cfadr\", \"mand\", \"sweep\", { field: \"tlb_end\", format: \"currency\", unit: \"m\" }, { field: \"mezz_end\", format: \"currency\", unit: \"m\" }, { field: \"cash_end\", format: \"currency\", unit: \"m\" }, { field: \"net_debt\", format: \"currency\", unit: \"m\" }, { field: \"leverage\", format: \"multiple\" }, { field: \"coverage\", format: \"multiple\" }]} sortable />\n\n## Sensitivity — sponsor IRR by exit multiple and leverage.\n\nTwenty-five full model re-runs over the base fixtures; the live deal knobs above\ndo not bend this grid (it sweeps the model as a function, not the params).\n\n<Table source=\"model.sensitivity\" columns={[\"exit_multiple\", \"tlb_turns\", { field: \"sponsor_equity\", format: \"currency\", unit: \"m\" }, { field: \"exit_equity\", format: \"currency\", unit: \"m\" }, { field: \"moic\", format: \"multiple\" }, { field: \"irr\", format: \"percent\" }]} sortable />\n\n<Callout tone=\"info\">\n  Breakeven exit multiple for the 20 percent hurdle:\n  <Value source=\"model.breakeven_exit_multiple\" />x — a goal seek recomputed\n  by the model, not a pasted number. The converged average-balance interest\n  variant lives in the model.debt_schedule_avg and model.returns_avg cells.\n</Callout>\n",
  "caldera/worksheets/checks.sheet.js": "import { cell, testCell, expectClose, conservation, permutationInvariance, scaleInvariance, property } from \"@reckoner/stdlib\";\n\n// Caldera Components LBO — the checks worksheet.\n//\n// Where the Excel \"before\" has a Checks sheet of IF(...,\"OK\",\"BREAK\") text cells\n// (visible only if you go look, gating nothing), the port pins the same\n// invariants as typed test cells with mandatory kinds — specification (oracle\n// values generated by generate.py), metamorphic (conservation / permutation),\n// and property (covenants, monotonicity, convergence).\n//\n// R3-373: the oracle reaches the tests through DECLARED TEST INPUTS —\n// `expected: \"fixtures.expected_values\"` arrives as auxiliary context for the\n// expect functions (the name is not a subject input, so nothing feeds the\n// formula), and `ops_holdout` substitutes the subject's `plan` input with the\n// downside holdout fixture, re-running the operating build over it. No inline\n// constants remain.\n\n// Credit-agreement terms (spec constants, mirroring the Excel Checks sheet).\nconst COV_LEVERAGE_MAX = 6.0;\nconst COV_COVERAGE_MIN = 2.0;\n\nfunction checkAll(checks) {\n  for (const c of checks) {\n    if (!c.pass) return c;\n  }\n  return { pass: true, message: `${checks.length} value(s) match the generated oracle` };\n}\n\n// ── LTM EBITDA ────────────────────────────────────────────────────────────────\n\nexport const ltm_vs_oracle = testCell({\n  kind: \"specification\",\n  subject: \"model.ltm_ebitda\",\n  inputs: { expected: \"fixtures.expected_values\" },\n  expect: ({ result, inputs }) => checkAll([\n    expectClose(result, inputs.expected[0].ltm_ebitda, { abs: 0.005 }),\n  ]),\n});\n\nexport const ltm_permutation = testCell({\n  kind: \"metamorphic\",\n  subject: \"model.ltm_ebitda\",\n  relation: permutationInvariance({ over: \"hist\" }),\n});\n\n// ── sources & uses ────────────────────────────────────────────────────────────\n\nexport const su_vs_oracle = testCell({\n  kind: \"specification\",\n  subject: \"model.sources_uses\",\n  inputs: { expected: \"fixtures.expected_values\" },\n  expect: ({ result, inputs }) => checkAll([\n    expectClose(result.entry_ev, inputs.expected[0].entry_ev, { abs: 0.005 }),\n    expectClose(result.sponsor_equity, inputs.expected[0].sponsor_equity, { abs: 0.005 }),\n    expectClose(result.uses, inputs.expected[0].uses_total, { abs: 0.005 }),\n  ]),\n});\n\nexport const su_balances = testCell({\n  kind: \"property\",\n  subject: \"model.sources_uses\",\n  expect: ({ result }) =>\n    Math.abs(result.sources - result.uses) < 0.005 && result.sponsor_equity > 0 && result.sponsor_equity / result.uses < 0.6\n      ? { pass: true, message: \"sources = uses; equity cheque positive and under 60% of uses\" }\n      : { pass: false, message: \"sources-uses = \" + (result.sources - result.uses) + \"; equity share \" + (result.sponsor_equity / result.uses).toFixed(3) },\n});\n\nexport const su_conserves = testCell({\n  kind: \"metamorphic\",\n  subject: \"model.sources_uses_row\",\n  relation: conservation({\n    components: [\"tlb0\", \"mezz0\", \"cash_used\", \"sponsor_equity\"],\n    equals: \"uses_total\",\n    tol: { abs: 1e-6 },\n  }),\n});\n\n// ── operating build ───────────────────────────────────────────────────────────\n\nexport const ops_vs_oracle = testCell({\n  kind: \"specification\",\n  subject: \"model.operating\",\n  inputs: { expected: \"fixtures.expected_values\" },\n  expect: ({ result, inputs }) => {\n    const fy31 = result[result.length - 1];\n    return checkAll([\n      expectClose(fy31.revenue, inputs.expected[0].fy2031_revenue, { abs: 0.005 }),\n      expectClose(fy31.ebitda, inputs.expected[0].fy2031_ebitda, { abs: 0.005 }),\n    ]);\n  },\n});\n\nexport const ops_holdout = testCell({\n  kind: \"specification\",\n  subject: \"model.operating\",\n  inputs: { plan: \"fixtures.ops_plan_holdout\", holdout: \"fixtures.expected_holdout\" },\n  expect: ({ result, inputs }) => {\n    // `plan` SUBSTITUTES (it names a subject input): the operating build re-runs\n    // over the downside plan with everything else live. `holdout` is auxiliary.\n    const fy27 = result[0];\n    const fy31 = result[result.length - 1];\n    const oracle = inputs.holdout[0];\n    return checkAll([\n      expectClose(fy27.revenue, oracle.fy2027_revenue, { abs: 0.005 }),\n      expectClose(fy31.revenue, oracle.fy2031_revenue, { abs: 0.005 }),\n      expectClose(fy31.ebitda, oracle.fy2031_ebitda, { abs: 0.005 }),\n    ]);\n  },\n});\n\nexport const ops_permutation = testCell({\n  kind: \"metamorphic\",\n  subject: \"model.operating\",\n  relation: permutationInvariance({ over: \"plan\" }),\n});\n\n// R3-374 unlocked this: LTM EBITDA is linear in the historical revenue base, but\n// the same rows carry margins — `leaves` scales the revenues only. (The operating\n// build itself is NOT a fit here: its `year` column is a numeric label that does\n// not scale — scale-invariance wants subjects whose outputs are genuinely linear.)\nexport const ltm_scales = testCell({\n  kind: \"metamorphic\",\n  subject: \"model.ltm_ebitda\",\n  relation: scaleInvariance({ over: \"hist\", by: 2, leaves: [\"fy2024\", \"fy2025\", \"fy2026\"] }),\n});\n\nexport const ops_shape = testCell({\n  kind: \"property\",\n  subject: \"model.operating\",\n  expect: ({ result }) => {\n    for (let i = 1; i < result.length; i += 1) {\n      if (!(result[i].revenue > result[i - 1].revenue)) {\n        return { pass: false, message: `revenue not strictly increasing at year ${result[i].year}` };\n      }\n      if (!(result[i].ebitda / result[i].revenue > result[i - 1].ebitda / result[i - 1].revenue)) {\n        return { pass: false, message: `blended margin not expanding at year ${result[i].year}` };\n      }\n    }\n    return { pass: true, message: \"revenue grows and blended margin expands every year (the plan's shape)\" };\n  },\n});\n\n// ── debt schedule (beginning-balance interest) ────────────────────────────────\n\nexport const sched_rolls = testCell({\n  kind: \"property\",\n  subject: \"model.debt_schedule\",\n  expect: ({ result }) => {\n    let prior = null;\n    for (const r of result) {\n      if (Math.abs(r.tlb_end - (r.tlb_begin - r.mand - r.sweep)) > 0.005) {\n        return { pass: false, message: `TLB roll-forward breaks in ${r.year}` };\n      }\n      if (Math.abs(r.net_debt - (r.tlb_end + r.mezz_end + r.rev_end - r.cash_end)) > 0.005) {\n        return { pass: false, message: `net debt identity breaks in ${r.year}` };\n      }\n      if (prior !== null) {\n        if (Math.abs(r.tlb_begin - prior.tlb_end) > 1e-9) {\n          return { pass: false, message: `TLB begin ≠ prior end in ${r.year}` };\n        }\n        if (Math.abs(r.cash_end - (prior.cash_end + r.retained)) > 0.005) {\n          return { pass: false, message: `cash roll-forward breaks in ${r.year}` };\n        }\n      }\n      prior = r;\n    }\n    return { pass: true, message: `roll-forward conserves across ${result.length} years` };\n  },\n});\n\nexport const sched_covenants = testCell({\n  kind: \"property\",\n  subject: \"model.debt_schedule\",\n  expect: ({ result, inputs }) => {\n    const minCash = inputs.a[0].min_cash;\n    for (const r of result) {\n      if (r.leverage > COV_LEVERAGE_MAX) return { pass: false, message: `leverage covenant breach in ${r.year}: ${r.leverage.toFixed(3)}x` };\n      if (r.coverage < COV_COVERAGE_MIN) return { pass: false, message: `coverage covenant breach in ${r.year}: ${r.coverage.toFixed(3)}x` };\n      if (r.cash_end < minCash) return { pass: false, message: `liquidity breach in ${r.year}: cash ${r.cash_end.toFixed(3)} < min ${minCash}` };\n      if (r.rev_end !== 0) return { pass: false, message: `revolver drawn in ${r.year} — base case is planned to never draw` };\n    }\n    return { pass: true, message: `leverage ≤ ${COV_LEVERAGE_MAX}x, coverage ≥ ${COV_COVERAGE_MIN}x, liquidity and undrawn revolver hold all ${result.length} years` };\n  },\n});\n\nexport const sched_permutation = testCell({\n  kind: \"metamorphic\",\n  subject: \"model.debt_schedule\",\n  relation: permutationInvariance({ over: \"ops\" }),\n});\n\nexport const sched_vs_oracle = testCell({\n  kind: \"specification\",\n  subject: \"model.debt_schedule\",\n  inputs: { expected: \"fixtures.expected_values\" },\n  expect: ({ result, inputs }) => {\n    const fy31 = result[result.length - 1];\n    const oracle = inputs.expected[0];\n    return checkAll([\n      expectClose(fy31.tlb_end, oracle.tlb_end_2031, { abs: 0.005 }),\n      expectClose(fy31.cash_end, oracle.cash_end_2031, { abs: 0.005 }),\n      expectClose(fy31.net_debt, oracle.net_debt_2031, { abs: 0.005 }),\n    ]);\n  },\n});\n\n// ── debt schedule (average-balance / converged circular) ─────────────────────\n\nexport const sched_avg_converges = testCell({\n  kind: \"property\",\n  subject: \"model.debt_schedule_avg\",\n  expect: ({ result }) => {\n    const total = result.reduce((acc, r) => acc + r.iterations, 0);\n    if (total <= 0) return { pass: false, message: \"no fixed-point iterations recorded\" };\n    if (total >= 200 * result.length) return { pass: false, message: \"hit the iteration cap — fixed point did not converge\" };\n    return { pass: true, message: `fixed point converged in ${total} iterations over ${result.length} years` };\n  },\n});\n\nexport const sched_avg_vs_oracle = testCell({\n  kind: \"specification\",\n  subject: \"model.debt_schedule_avg\",\n  inputs: { expected: \"fixtures.expected_values\" },\n  expect: ({ result, inputs }) => {\n    const fy31 = result[result.length - 1];\n    return checkAll([\n      expectClose(fy31.net_debt, inputs.expected[0].net_debt_2031_avg, { abs: 0.005 }),\n    ]);\n  },\n});\n\n// ── returns ───────────────────────────────────────────────────────────────────\n\nexport const returns_vs_oracle = testCell({\n  kind: \"specification\",\n  subject: \"model.returns\",\n  inputs: { expected: \"fixtures.expected_values\" },\n  expect: ({ result, inputs }) => checkAll([\n    expectClose(result.exit_equity, inputs.expected[0].exit_equity, { abs: 0.005 }),\n    expectClose(result.moic, inputs.expected[0].moic, { abs: 1e-4 }),\n    expectClose(result.irr, inputs.expected[0].irr, { abs: 1e-6 }),\n  ]),\n});\n\nexport const returns_beats_hurdle = testCell({\n  kind: \"property\",\n  subject: \"model.returns\",\n  expect: ({ result, inputs }) => {\n    const hurdle = inputs.a[0].hurdle_irr;\n    return result.irr >= hurdle\n      ? { pass: true, message: `base IRR ${(result.irr * 100).toFixed(2)}% clears the ${(hurdle * 100).toFixed(0)}% hurdle` }\n      : { pass: false, message: `base IRR ${(result.irr * 100).toFixed(2)}% below the ${(hurdle * 100).toFixed(0)}% hurdle` };\n  },\n});\n\nexport const returns_avg_vs_oracle = testCell({\n  kind: \"specification\",\n  subject: \"model.returns_avg\",\n  inputs: { expected: \"fixtures.expected_values\" },\n  expect: ({ result, inputs }) => checkAll([\n    expectClose(result.irr, inputs.expected[0].irr_avg, { abs: 1e-6 }),\n  ]),\n});\n\nexport const returns_avg_direction = testCell({\n  kind: \"property\",\n  subject: \"model.returns_avg\",\n  inputs: { expected: \"fixtures.expected_values\" },\n  expect: ({ result, inputs }) =>\n    result.irr > inputs.expected[0].irr\n      ? { pass: true, message: \"average-balance interest is cheaper than beginning-balance (balances decline), so IRR is higher\" }\n      : { pass: false, message: \"avg-balance IRR not above begin-balance IRR\" },\n});\n\n// ── sensitivity grid ──────────────────────────────────────────────────────────\n\nexport const grid_center_matches_base = testCell({\n  kind: \"specification\",\n  subject: \"model.sensitivity\",\n  inputs: { expected: \"fixtures.expected_values\" },\n  expect: ({ result, inputs }) => {\n    const center = result.find((r) => r.exit_multiple === 8.0 && r.tlb_turns === 5.0);\n    if (center === undefined) return { pass: false, message: \"grid is missing the (8.0x, 5.0x) center\" };\n    return checkAll([expectClose(center.irr, inputs.expected[0].irr, { abs: 1e-9 })]);\n  },\n});\n\nexport const grid_monotone = testCell({\n  kind: \"property\",\n  subject: \"model.sensitivity\",\n  expect: ({ result }) => {\n    const byKey = new Map(result.map((r) => [`${r.exit_multiple}|${r.tlb_turns}`, r]));\n    const mults = [7.0, 7.5, 8.0, 8.5, 9.0];\n    const levs = [4.0, 4.5, 5.0, 5.5, 6.0];\n    for (const lev of levs) {\n      for (let i = 1; i < mults.length; i += 1) {\n        const a = byKey.get(`${mults[i - 1]}|${lev}`);\n        const b = byKey.get(`${mults[i]}|${lev}`);\n        if (!(b.irr > a.irr)) return { pass: false, message: `IRR not increasing in exit multiple at leverage ${lev}x` };\n      }\n    }\n    for (const m of mults) {\n      for (let j = 1; j < levs.length; j += 1) {\n        const a = byKey.get(`${m}|${levs[j - 1]}`);\n        const b = byKey.get(`${m}|${levs[j]}`);\n        if (!(b.irr > a.irr)) return { pass: false, message: `IRR not increasing in leverage at exit ${m}x` };\n        if (!(b.sponsor_equity < a.sponsor_equity)) return { pass: false, message: `equity not decreasing in leverage at exit ${m}x` };\n      }\n    }\n    return { pass: true, message: \"IRR increases in exit multiple and in leverage; the equity cheque shrinks with leverage (25 re-runs)\" };\n  },\n});\n\n// ── breakeven (goal seek) ─────────────────────────────────────────────────────\n\nexport const breakeven_vs_oracle = testCell({\n  kind: \"specification\",\n  subject: \"model.breakeven_exit_multiple\",\n  inputs: { expected: \"fixtures.expected_values\" },\n  expect: ({ result, inputs }) => checkAll([\n    expectClose(result, inputs.expected[0].breakeven_exit_multiple, { abs: 1e-6 }),\n  ]),\n});\n\nexport const breakeven_implication = testCell({\n  kind: \"property\",\n  subject: \"model.breakeven_exit_multiple\",\n  inputs: { expected: \"fixtures.expected_values\" },\n  expect: ({ result, inputs }) => {\n    const hurdle = inputs.a[0].hurdle_irr;\n    const baseIrr = inputs.expected[0].irr;\n    const baseExit = inputs.a[0].exit_multiple;\n    if (baseIrr >= hurdle && !(result <= baseExit)) {\n      return { pass: false, message: `base IRR clears the hurdle but breakeven ${result.toFixed(3)}x exceeds the exit multiple ${baseExit}x` };\n    }\n    if (baseIrr < hurdle && !(result > baseExit)) {\n      return { pass: false, message: `base IRR misses the hurdle but breakeven ${result.toFixed(3)}x is at or below the exit multiple` };\n    }\n    return { pass: true, message: \"breakeven sits on the correct side of the base exit multiple given IRR vs hurdle\" };\n  },\n});\n",
  "caldera/worksheets/model.sheet.js": "import { cell, table, rollforward, cumprod, irr, solve, fixpoint, sum } from \"@reckoner/stdlib\";\n\n// Caldera Components LBO — the model worksheet.\n//\n// Structure mirrors the Excel \"before\" (caldera_lbo.xlsx) tab for tab:\n//   Assumptions        → fixtures.assumptions + the three params.* deal knobs\n//   Operating_Model    → `ltm_ebitda`, `operating`\n//   Sources_Uses       → `sources_uses`\n//   Debt_Schedule      → `debt_schedule` (begin-balance interest) and\n//                        `debt_schedule_avg` (average-balance interest — Excel's\n//                        iterative-calc circular reference, here an explicit,\n//                        per-year converged fixed point inside one cell)\n//   Sens_Models/grid   → `sensitivity` (25 full model re-runs)\n//   Returns            → `returns`, `sponsor_irr`, `moic`, `breakeven_exit_multiple`\n//\n// Naming note: a worksheet export shadows any same-named stdlib callable in module\n// scope (the import is stripped; stdlib functions arrive as compartment globals) —\n// hence `sponsor_irr`, not `irr`, for the scalar tile.\n//\n// Everything below is pure: plain values in, plain values out, no ambient\n// anything. The module-scope helpers are the \"model as a function\" the\n// sensitivity grid needs — a cell cannot re-run other cells, so the model must\n// decompose into callable pure functions (the architectural finding this case\n// study exists to document).\n\nconst YEARS_ORDER = \"year\";\n\n// ── module-scope model helpers (shared by cells and the grid) ────────────────\n// (The hand-rolled npv/irrBisect that lived here is R3-376's receipt: the stdlib's\n// irr() replaced it, deleted rather than kept.)\n\n// The operating build: segment revenue paths (base × the compounded growth factors via\n// cumprod), margin-weighted EBITDA, D&A / capex / ΔNWC per year. Fluent stdlib throughout.\nfunction buildOperating(hist, plan, yearPlan, a) {\n  const baseTotal = hist.reduce((acc, h) => acc + h.fy2026, 0);\n  const paths = table(hist)\n    .join(plan, { on: \"segment\" })\n    .derive({ growth_factor: (r) => 1 + r.growth })\n    .scan(\n      { factor_path: cumprod(\"growth_factor\") },\n      { partitionBy: \"segment\", orderBy: YEARS_ORDER },\n    )\n    .derive({ seg_revenue: (r) => r.fy2026 * r.factor_path, seg_ebitda: (r) => r.fy2026 * r.factor_path * r.margin })\n    .groupBy(\"year\")\n    .rollup({ revenue: sum(\"seg_revenue\"), ebitda: sum(\"seg_ebitda\") })\n    .rows();\n  return table(paths)\n    .sort(YEARS_ORDER)\n    .lag(\"revenue\", { as: \"prior_revenue\", fill: baseTotal })\n    .join(yearPlan, { on: \"year\" })\n    .derive({\n      da: (r) => a.da_pct * r.revenue,\n      capex: (r) => r.capex_pct * r.revenue,\n      delta_nwc: (r) => a.nwc_pct * (r.revenue - r.prior_revenue),\n    })\n    .rows()\n    .map((r) => ({\n      year: r.year, revenue: r.revenue, ebitda: r.ebitda,\n      da: r.da, capex: r.capex, delta_nwc: r.delta_nwc,\n    }));\n}\n\nfunction sourcesUses(ltm, a, entryMultiple, tlbTurns) {\n  const ev = entryMultiple * ltm;\n  const purchase_equity = ev - a.existing_debt + a.existing_cash;\n  const tlb0 = tlbTurns * ltm;\n  const mezz0 = a.mezz_turns * ltm;\n  const txn_fees = a.txn_fee_pct * ev;\n  const fin_fees = a.fin_fee_pct * (tlb0 + mezz0);\n  const uses = purchase_equity + a.existing_debt + txn_fees + fin_fees;\n  const cash_used = a.existing_cash - a.min_cash;\n  const sponsor_equity = uses - tlb0 - mezz0 - cash_used;\n  return {\n    entry_ev: ev, purchase_equity, tlb0, mezz0, txn_fees, fin_fees,\n    uses, cash_used, sponsor_equity, sources: tlb0 + mezz0 + cash_used + sponsor_equity,\n  };\n}\n\n// One year of the debt waterfall given opening balances. mode \"begin\" charges\n// interest on beginning balances; mode \"avg\" on average balances, where `est`\n// is the current fixed-point estimate of the ending balances (the caller\n// iterates). Mirrors generate.py `year_pass` operation for operation.\nfunction yearPass(op, a, su, bal, mode) {\n  let e_tlb, e_mezz, e_rev, e_undrawn;\n  if (mode === \"begin\") {\n    e_tlb = bal.tlb; e_mezz = bal.mezz; e_rev = bal.rev;\n    e_undrawn = Math.max(0, a.revolver_capacity - bal.rev);\n  } else {\n    e_tlb = (bal.tlb + bal.est.tlb) / 2;\n    e_mezz = (bal.mezz + bal.est.mezz) / 2;\n    e_rev = (bal.rev + bal.est.rev) / 2;\n    e_undrawn = Math.max(0, (a.revolver_capacity - bal.rev + a.revolver_capacity - bal.est.rev) / 2);\n  }\n  const interest = a.tlb_rate * e_tlb + a.mezz_cash_rate * e_mezz + a.revolver_rate * e_rev + a.commitment_fee * e_undrawn;\n  const ebt = op.ebitda - op.da - interest;\n  const tax = Math.max(0, ebt * a.tax_rate);\n  const ni = ebt - tax;\n  const cfadr = ni + op.da - op.capex - op.delta_nwc;\n  const rev_repay = Math.min(bal.rev, Math.max(0, cfadr));\n  const rev_draw = Math.min(Math.max(0, a.revolver_capacity - bal.rev), Math.max(0, -cfadr));\n  const res1 = cfadr - rev_repay + rev_draw;\n  const mand = Math.min(a.tlb_amort_pct * su.tlb0, bal.tlb);\n  let res2 = res1 - mand;\n  const draw2 = Math.min(Math.max(0, a.revolver_capacity - (bal.rev + rev_draw)), Math.max(0, -res2));\n  res2 = res2 + draw2;\n  const sweep = Math.min(a.cash_sweep_pct * Math.max(0, res2), bal.tlb - mand);\n  const end = {\n    rev: bal.rev + rev_draw + draw2 - rev_repay,\n    tlb: bal.tlb - mand - sweep,\n    mezz: bal.mezz * (1 + a.mezz_pik_rate),\n    cash: bal.cash + res2 - sweep,\n  };\n  return {\n    begin: { rev: bal.rev, tlb: bal.tlb, mezz: bal.mezz, cash: bal.cash },\n    interest, ebt, tax, ni, cfadr, mand, sweep, retained: res2 - sweep,\n    rev_draw: rev_draw + draw2, rev_repay, end,\n  };\n}\n\n// The 5-year roll-forward on the stdlib's multi-state primitive (R3-375): the\n// state tuple {rev, tlb, mezz, cash} evolves through each year's waterfall —\n// the structure Excel unrolls across columns. 'avg' mode iterates each year's\n// step to a fixed point (Excel's iterative-calc circular reference, explicit).\nfunction scheduleYears(ops, a, su, mode) {\n  let iterations = 0;\n  return rollforward(ops, {\n    orderBy: YEARS_ORDER,\n    begin: { rev: 0, tlb: su.tlb0, mezz: su.mezz0, cash: a.min_cash },\n    step: (op, bal) => {\n      let out;\n      if (mode === \"begin\") {\n        out = yearPass(op, a, su, bal, \"begin\");\n      } else {\n        // The within-year fixed point on the stdlib's fixpoint helper (R3-378):\n        // iterate the average-balance estimate until it stops moving, then take\n        // one final pass at the converged state so the year's output columns are\n        // exactly consistent with it. Non-convergence surfaces as a thrown error\n        // — visible, the opposite of Excel's silent iterative calc.\n        const trial = { rev: bal.rev, tlb: bal.tlb, mezz: bal.mezz, cash: bal.cash, est: null };\n        let lastOut = null;\n        const fp = fixpoint(\n          { rev: bal.rev, tlb: bal.tlb, mezz: bal.mezz, cash: bal.cash },\n          (est) => {\n            trial.est = est;\n            const pass = yearPass(op, a, su, trial, \"avg\");\n            lastOut = pass;\n            return pass.end;\n          },\n          { tol: 1e-12, maxIterations: 200 },\n        );\n        if (!fp.converged) {\n          throw new Error(\"average-balance interest did not converge in year \" + op.year);\n        }\n        iterations += fp.iterations;\n        out = lastOut;\n      }\n      const netDebt = out.end.tlb + out.end.mezz + out.end.rev - out.end.cash;\n      return {\n        out: {\n          interest: out.interest, cfadr: out.cfadr, mand: out.mand, sweep: out.sweep,\n          retained: out.retained, rev_draw: out.rev_draw, rev_repay: out.rev_repay,\n          net_debt: netDebt, leverage: netDebt / op.ebitda, coverage: op.ebitda / out.interest,\n          iterations,\n        },\n        next: out.end,\n      };\n    },\n  });\n}\n\n// The whole model as one pure function of the fixtures + overrides — what the\n// sensitivity grid maps over and the breakeven goal-seek bisects on. This\n// decomposition is *forced* by Reckoner's no-ambient-registry rule: a cell\n// cannot re-run other cells with different params, so the model must be a\n// function before it can be swept.\nfunction runModel(a, hist, plan, yearPlan, o) {\n  const overrides = o || {};\n  const ltm = hist.reduce((acc, h) => acc + h.fy2026 * h.margin_fy2026, 0);\n  const ops = buildOperating(hist, plan, yearPlan, a);\n  const su = sourcesUses(ltm, a,\n    overrides.entry_multiple === undefined ? a.entry_multiple : overrides.entry_multiple,\n    overrides.tlb_turns === undefined ? a.tlb_turns : overrides.tlb_turns);\n  const sched = scheduleYears(ops, a, su, \"begin\");\n  const last = ops[ops.length - 1];\n  const lastS = sched[sched.length - 1];\n  const exitMultiple = overrides.exit_multiple === undefined ? a.exit_multiple : overrides.exit_multiple;\n  const exit_ev = exitMultiple * last.ebitda;\n  const exit_equity = exit_ev - lastS.net_debt;\n  const flows = [-su.sponsor_equity].concat(Array(a.hold_years - 1).fill(0)).concat([exit_equity]);\n  return {\n    su, exit_ev, net_debt: lastS.net_debt, exit_equity,\n    moic: exit_equity / su.sponsor_equity, irr: irr(flows),\n  };\n}\n\n// Goal seek: the exit multiple at which IRR = hurdle (IRR is increasing in it).\n// Excel does this interactively (Data → What-If → Goal Seek); here it is an\n// explicit bisection, so it recomputes with the model and is testable.\nfunction breakevenExitMultiple(a, hist, plan, yearPlan, hurdle) {\n  // Goal seek on the stdlib's solve (R3-381): IRR is increasing in the exit\n  // multiple, so the bracket is monotone and bisection is exact.\n  return solve((m) => runModel(a, hist, plan, yearPlan, { exit_multiple: m }).irr, hurdle, 4.0, 12.0);\n}\n\n// ── cells ─────────────────────────────────────────────────────────────────────\n\nexport const ltm_ebitda = cell({\n  doc: \"LTM (FY2026) EBITDA at close, EUR m — the entry multiple's base\",\n  inputs: { hist: \"fixtures.historical_segments\" },\n  formula: ({ hist }) => hist.reduce((acc, h) => acc + h.fy2026 * h.margin_fy2026, 0),\n});\n\nexport const operating = cell({\n  doc: \"Five-year operating build: revenue by chained segment growth, margin-weighted EBITDA, D&A, capex, ΔNWC — one row per year FY2027–FY2031\",\n  inputs: {\n    hist: \"fixtures.historical_segments\",\n    plan: \"fixtures.ops_plan\",\n    yearPlan: \"fixtures.year_plan\",\n    a: \"fixtures.assumptions\",\n  },\n  formula: ({ hist, plan, yearPlan, a }) => buildOperating(hist, plan, yearPlan, a[0]),\n});\n\nexport const sources_uses = cell({\n  doc: \"Sources & uses at close (EUR m): entry EV, purchase equity, fees, funded debt, and the sponsor equity plug. The interactive deal knobs are params\",\n  inputs: { ltm: \"model.ltm_ebitda\", a: \"fixtures.assumptions\", entry: \"params.entry_multiple\", tlbTurns: \"params.tlb_turns\" },\n  formula: ({ ltm, a, entry, tlbTurns }) => sourcesUses(ltm, a[0], entry, tlbTurns),\n});\n\n// One-row projection of sources_uses shaped for the conservation test:\n// components (debt + cash + equity) must sum to total uses, row-wise.\nexport const sources_uses_row = cell({\n  doc: \"Sources & uses as a single conservation row: tlb0 + mezz0 + cash_used + sponsor_equity = uses\",\n  inputs: { su: \"model.sources_uses\" },\n  formula: ({ su }) => [{\n    tlb0: su.tlb0, mezz0: su.mezz0, cash_used: su.cash_used,\n    sponsor_equity: su.sponsor_equity, uses_total: su.uses,\n  }],\n});\n\nexport const debt_schedule = cell({\n  doc: \"Debt schedule FY2027–FY2031, interest on beginning balances (no circularity): revolver draw/repay, TLB mandatory amortisation + 75% cash sweep, mezzanine PIK accrual, cash build, leverage and coverage\",\n  inputs: { ops: \"model.operating\", su: \"model.sources_uses\", a: \"fixtures.assumptions\" },\n  formula: ({ ops, su, a }) => scheduleYears(ops, a[0], su, \"begin\"),\n});\n\nexport const debt_schedule_avg = cell({\n  doc: \"Debt schedule with interest on AVERAGE balances — Excel's iterative-calc circular reference, expressed as an explicit per-year fixed point converged to 1e-12; rows carry the iteration count\",\n  inputs: { ops: \"model.operating\", su: \"model.sources_uses\", a: \"fixtures.assumptions\" },\n  formula: ({ ops, su, a }) => scheduleYears(ops, a[0], su, \"avg\"),\n});\n\nexport const returns = cell({\n  doc: \"Exit returns at FY2031: exit EV at the exit multiple, less net debt, → sponsor equity value, MOIC and IRR (bisection on the annual flows)\",\n  inputs: {\n    ops: \"model.operating\", sched: \"model.debt_schedule\", su: \"model.sources_uses\",\n    a: \"fixtures.assumptions\", exit: \"params.exit_multiple\",\n  },\n  formula: ({ ops, sched, su, a, exit }) => {\n    const last = ops[ops.length - 1];\n    const lastS = sched[sched.length - 1];\n    const exit_ev = exit * last.ebitda;\n    const exit_equity = exit_ev - lastS.net_debt;\n    const flows = [-su.sponsor_equity].concat(Array(a[0].hold_years - 1).fill(0)).concat([exit_equity]);\n    return {\n      exit_ev, net_debt: lastS.net_debt, exit_equity, sponsor_equity: su.sponsor_equity,\n      moic: exit_equity / su.sponsor_equity, irr: irr(flows),\n    };\n  },\n});\n\nexport const returns_avg = cell({\n  doc: \"Exit returns under the average-balance (converged circular) debt schedule — the answer Excel's iterative sheet would give\",\n  inputs: {\n    ops: \"model.operating\", sched: \"model.debt_schedule_avg\", su: \"model.sources_uses\",\n    a: \"fixtures.assumptions\", exit: \"params.exit_multiple\",\n  },\n  formula: ({ ops, sched, su, a, exit }) => {\n    const last = ops[ops.length - 1];\n    const lastS = sched[sched.length - 1];\n    const exit_ev = exit * last.ebitda;\n    const exit_equity = exit_ev - lastS.net_debt;\n    const flows = [-su.sponsor_equity].concat(Array(a[0].hold_years - 1).fill(0)).concat([exit_equity]);\n    return { exit_ev, exit_equity, irr: irr(flows), moic: exit_equity / su.sponsor_equity };\n  },\n});\n\n// Thin named scalars the template and tests bind to.\nexport const sponsor_irr = cell({\n  doc: \"Base-case sponsor IRR as a scalar, for the Kpi tile\",\n  inputs: { r: \"model.returns\" },\n  formula: ({ r }) => r.irr,\n});\n\nexport const moic = cell({\n  doc: \"Base-case sponsor MOIC (x) as a scalar, for the Kpi tile\",\n  inputs: { r: \"model.returns\" },\n  formula: ({ r }) => r.moic,\n});\n\nexport const sponsor_equity = cell({\n  doc: \"Sponsor equity cheque at close (EUR m), for the Kpi tile\",\n  inputs: { su: \"model.sources_uses\" },\n  formula: ({ su }) => su.sponsor_equity,\n});\n\nexport const sensitivity = cell({\n  doc: \"Two-way sensitivity: sponsor IRR and MOIC for exit multiple 7.0–9.0x × TLB leverage 4.0–6.0x — 25 full model re-runs over the base fixtures (not the live params)\",\n  inputs: {\n    a: \"fixtures.assumptions\", hist: \"fixtures.historical_segments\",\n    plan: \"fixtures.ops_plan\", yearPlan: \"fixtures.year_plan\",\n  },\n  formula: ({ a, hist, plan, yearPlan }) => {\n    const exitMults = [7.0, 7.5, 8.0, 8.5, 9.0];\n    const levs = [4.0, 4.5, 5.0, 5.5, 6.0];\n    const rows = [];\n    for (const exit_m of exitMults) {\n      for (const lev of levs) {\n        const r = runModel(a[0], hist, plan, yearPlan, { exit_multiple: exit_m, tlb_turns: lev });\n        rows.push({\n          exit_multiple: exit_m, tlb_turns: lev,\n          sponsor_equity: r.su.sponsor_equity, exit_equity: r.exit_equity,\n          moic: r.moic, irr: r.irr,\n        });\n      }\n    }\n    return rows;\n  },\n});\n\nexport const breakeven_exit_multiple = cell({\n  doc: \"Goal seek: the exit multiple at which the sponsor IRR equals the 20% hurdle (IRR is monotone in the exit multiple)\",\n  inputs: {\n    a: \"fixtures.assumptions\", hist: \"fixtures.historical_segments\",\n    plan: \"fixtures.ops_plan\", yearPlan: \"fixtures.year_plan\",\n  },\n  formula: ({ a, hist, plan, yearPlan }) => breakevenExitMultiple(a[0], hist, plan, yearPlan, a[0].hurdle_irr),\n});\n",
};
